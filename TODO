1) Change attachPoint and parentAttachPoint so that they represent a fraction of parent height/width (values from -1.0 to 1.0)

2) Figure out a good way for machine parts to collide

3) Create a file format for machines so we can write interesting ones to disk
